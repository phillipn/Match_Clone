<%= form_for [@match_room, message], remote: true, authenticity_token: true do |f| %>
  <%= f.text_area :content, class: "form-control", data: {textarea: "content"} %>

  <%= f.submit "Submit", class: "btn btn-primary message_submit" %>
<% end %>
